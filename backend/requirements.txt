sqlalchemy>=2.0
faker>=20.0
psycopg2-binary>=2.9   # only needed when DATABASE_URL points at Postgres
neo4j>=5.0
networkx>=3.0          # default GRAPH_BACKEND — required even without Neo4j
scipy>=1.11            # Stage G1: required by networkx.pagerank() for offender network_profile centrality
langgraph>=0.2
langchain-anthropic>=0.2
anthropic>=0.40
fastapi>=0.110
python-multipart>=0.0.9 # Stage D, Sprint 3 — required by FastAPI for UploadFile (audio) endpoints
uvicorn[standard]>=0.29
pydantic>=2.0
chromadb>=0.5
reportlab>=4.0          # PDF investigation report export
requests>=2.31          # Stage D, Sprint 3 — outbound calls to pluggable cloud STT/TTS providers
fonttools>=4.50         # Stage D, Sprint 4 — validates the bundled Kannada font's glyph coverage
                        # (also used by validate_stage_d4.py; not required at runtime for PDF export itself)
pyjwt>=2.8              # Stage E1 — JWT access tokens
argon2-cffi>=23.1       # Stage E1 — argon2id password hashing
slowapi>=0.1.9          # Stage E6 — rate limiting (installed now, wired up in Sprint E6)
